=breadcrumb_tag do
  %li
    Сотрудники
  %li
    = link_to 'Сотрудники', empls_path
  %li.active
    = t('headers.empls.Редактирование сотрудника')
  %li
    Администрирование

.page-header
  %h1= t('headers.empls.Редактирование сотрудника')
= link_to  projects_path, class: 'btn btn-warning' do
  = t('buttons.Назад')

= render 'form'

%h3 Выбор другого отдела
= form_for @empl, url: change_depart_empl_path(@empl), method: :get do |f|
  .row
    .col-md-6
      .form-group
        = f.label :depart
        = f.select :depart_id, Depart.all.map{|x| [x.d_name, x.id]},
          {}, {class: 'form-control'}
        = f.error_messages_for(:depart_id)
       
  
